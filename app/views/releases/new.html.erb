<h1>New release</h1>

<% form_for(@release) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => 'tasks/simple_form', :locals => {:f => f}%>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', releases_path %>
