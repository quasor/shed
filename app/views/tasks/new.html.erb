
<% form_for(@task) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form', :locals => {:f => f}%>
	<%= f.submit "Save", :id => 'submit' %>
    <%= link_to 'Back', tasks_path %>
<% end %>

