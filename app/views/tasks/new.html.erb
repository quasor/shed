
<% form_for(@task) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form', :locals => {:f => f}%>
  <p>
		<%= link_to 'Back', tasks_path, :style => "float:right" %>
    <%= f.submit "Save" %>
  </p>
<% end %>

