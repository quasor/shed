<h1>New project</h1>

<% form_for(@project) do |f| %>
  <%= f.error_messages %>

<%= render :partial => 'tasks/simple_form', :locals => {:f => f}%>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', projects_path %>
